[user]
	email = codazendev@gmail.com
	name = Jeremy King

[init]
    # Tell git to use a default branch name of "main" instead of "master".
	defaultBranch = main

[alias]
    # Alias to change the user name and email for the current repository to my codazendev accounts.
    usec = !git config user.name 'Jeremy King' && git config user.email 'codazendev@gmail.com'

    # Alias to change the user name and email for the current repository to my jgkingdev accounts.
    usej = !git config user.name 'Jeremy King' && git config user.email 'jeremykingdev@gmail.com'

[pull]
    rebase = true
